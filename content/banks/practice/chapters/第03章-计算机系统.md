# 综合知识 · 第03章 计算机系统

共 53 题。

---

### 41. [basic] 嵌入式实时控制芯片更强调固定短指令与深度流水线，典型设计取向是（ ）。

- A. CISC 复杂指令集
- B. RISC 精简指令集
- C. VLIW 与 CISC 相同
- D. 模拟计算机架构

<!-- ANS B | CISC与RISC | basic -->

### 42. [basic] CPU 主频 3GHz，主存延迟约 100ns，引入 L1/L2 Cache 的主要目的是（ ）。

- A. 扩大物理磁盘容量
- B. 缩小 CPU 与主存速度差距
- C. 替代虚拟存储页表
- D. 实现进程间通信

<!-- ANS B | Cache作用 | basic -->

### 43. [basic] 某 Cache 每个主存块只能映射到唯一行，冲突时直接替换，该方式属于（ ）。

- A. 全相联映射
- B. 直接映射
- C. 组相联映射
- D. 段页式映射

<!-- ANS B | Cache映射 | basic -->

### 44. [basic] 磁盘大批量读入内存时，采用 DMA 方式相较 CPU 逐字节轮询 I/O 的优势是（ ）。

- A. 提高 CPU 占用率
- B. 外设与主存直接传输，减少 CPU 干预
- C. 取消中断机制
- D. 只能用于键盘输入

<!-- ANS B | DMA | basic -->

### 45. [basic] 生产者-消费者共享有界缓冲区，需保证「空位与产品数」互斥同步，经典机制是（ ）。

- A. 仅用关中断
- B. 信号量 P/V 操作
- C. 仅 busy-wait 自旋
- D. 增大内存即可

<!-- ANS B | PV操作 | basic -->

### 46. [basic] 数据库服务器要求一块磁盘故障不丢数据且读性能提升，较合适的是（ ）。

- A. RAID 0 条带化
- B. RAID 1 镜像
- C. RAID 0 无冗余
- D. 单盘存储

<!-- ANS B | RAID | basic -->

### 47. [basic] 多核服务器中各 CPU 对称共享内存与 I/O 总线，操作系统任意核均可调度任务，属于（ ）。

- A. SMP 对称多处理
- B. NUMA 非统一访存
- C. 单核分时系统
- D. 分布式集群节点

<!-- ANS A | SMP | basic -->

### 48. [basic] 网卡收到数据包后向 CPU 发信号，OS 暂停当前线程转去执行驱动程序，该机制是（ ）。

- A. 轮询
- B. 中断
- C. 通道程序
- D. 虚拟化陷出

<!-- ANS B | 中断 | basic -->

### 49. [basic] 现代 PC 将程序指令与数据以二进制形式存放在同一主存，按顺序取指执行，属于（ ）。

- A. 哈佛结构双总线分离存储
- B. 冯·诺依曼存储程序结构
- C. 纯模拟计算架构
- D. 仅堆栈机无存储器

<!-- ANS B | 冯·诺依曼结构 | basic -->

### 50. [basic] L2 Cache 命中率从 90% 提升到 98%，在 Cache 命中时间与主存访问时间不变时，平均访存时间通常（ ）。

- A. 升高
- B. 降低
- C. 不变
- D. 先升后降

<!-- ANS B | 命中率 | basic -->

### 51. [basic] 循环数组顺序访问时，相邻元素很可能仍在 Cache 中，体现的是（ ）。

- A. 时间局部性
- B. 空间局部性
- C. 随机访问性
- D. 写时复制

<!-- ANS B | 局部性原理 | basic -->

### 52. [basic] 两进程各持有一把锁并互相等待对方释放，形成永久阻塞。破坏「循环等待」可通过（ ）。

- A. 允许抢占已占资源
- B. 按统一顺序申请资源
- C. 取消互斥条件
- D. 忽略同步直接并发

<!-- ANS B | 死锁四个必要条件 | basic -->

### 53. [basic] 进程逻辑地址空间 8GB，物理内存 16GB 仍可使用，依赖（ ）。

- A. 虚拟存储与页式地址变换
- B. 仅增大 Cache
- C. 取消页表
- D. 禁止换页

<!-- ANS A | 虚拟存储 | basic -->

### 54. [basic] 同一进程内多线程共享地址空间，但各自独立调度执行，说明（ ）。

- A. 线程是资源分配单位
- B. 进程是 CPU 调度基本单位
- C. 线程是 CPU 调度基本单位，进程是资源分配单位
- D. 进程与线程职责相同

<!-- ANS C | 进程与线程 | basic -->

### 55. [basic] 大型主机将成批磁带到主存的数据搬运交给专用 I/O 处理器执行，CPU 仅发启动命令，该部件是（ ）。

- A. DMA 控制器
- B. 通道
- C. 通用 ALU
- D. 一级高速缓存（L1 Cache）

<!-- ANS B | 通道 | basic -->

### 56. [basic] 物理页框不足时，操作系统需选取牺牲页换出，下列（ ）不属于常见页面置换算法。

- A. FIFO 先进先出
- B. LRU 最近最少使用
- C. Clock 二次机会
- D. 将全部脏页写回但不选择牺牲页

<!-- ANS D | 页面置换 | basic -->

### 57. [deep] CPU 写 Cache 采用 write-back：命中时只改 Cache 标记脏块，替换时才写回主存。相比 write-through 主要优势是（ ）。

- A. 减少写主存次数，写性能更好
- B. 主存始终与 Cache 同步
- C. 无需脏位管理
- D. 完全消除 Cache 一致性协议

<!-- ANS A | 写穿透/写回 | deep -->

### 58. [deep] Cache命中率0.9，命中时间10ns，缺失代价100ns，则平均访存时间约为（ ）。

- A. 19ns（英文缩写，见解析中文说明）
- B. 100ns（英文缩写，见解析中文说明）
- C. 90ns（英文缩写，见解析中文说明）
- D. 10ns（英文缩写，见解析中文说明）

<!-- ANS A | Cache平均访存时间 | deep -->

### 59. [deep] 某矩阵乘法程序按行访问连续元素时 Cache 命中率显著高于随机访问。Cache 有效的根本原因是程序具有（ ）。

- A. 时间局部性与空间局部性
- B. 仅指令流水线并行
- C. 虚拟内存分页机制
- D. 多核对称调度

<!-- ANS A | 局部性 | deep -->

### 60. [deep] 某磁盘控制器以 DMA 传输 4MB 数据期间，总线仲裁允许 CPU 继续执行其他进程指令。DMA 相比 CPU 轮询搬运的主要作用是（ ）。

- A. 提高磁盘物理转速
- B. 减轻 CPU 参与数据搬运的负担
- C. 替代文件系统缓存
- D. 自动压缩传输数据

<!-- ANS B | DMA与CPU | deep -->

### 61. [deep] 某双路 NUMA 服务器上，线程访问本地节点内存延迟 80ns，跨节点 140ns。若某计算密集型任务平均访存延迟需从 120ns 降至 90ns 以下，较合理的优化是（ ）。

- A. 尽量绑定 CPU 与本地内存分配
- B. 禁用所有 CPU 缓存
- C. 强制随机跨节点分配
- D. 仅增加磁盘 swap 空间

<!-- ANS A | NUMA | deep -->

### 62. [deep] 某文件服务器 5 块盘组 RAID5，任意 1 块盘故障数据可重建，读并行较好但写需更新校验。与 RAID0 相比，该级别最突出的特点是（ ）。

- A. 无冗余纯条带化
- B. 分布式奇偶校验，容许单盘故障
- C. 镜像双盘完全复制
- D. 需至少 4 块盘做双校验

<!-- ANS B | RAID5特点 | deep -->

### 63. [deep] 进程频繁切换导致页表访问增多，CPU 用 TLB 缓存最近使用的页表项加速虚实地址转换。TLB 属于（ ）。

- A. 页表项的高速缓存
- B. 磁盘 swap 分区
- C. L3 数据 Cache
- D. 指令流水线寄存器

<!-- ANS A | TLB | deep -->

### 64. [medium] 某 OS 为破坏死锁「占有且等待」条件，规定进程在运行前一次性申请所需全部资源，否则等待。该策略属于（ ）。

- A. 死锁预防
- B. 死锁避免（银行家算法）
- C. 死锁检测与恢复
- D. 鸵鸟策略忽略死锁

<!-- ANS A | 死锁预防 | medium -->

### 65. [deep] 某嵌入式系统中，定时器中断服务期间发生更高优先级磁盘中断，CPU 先响应磁盘再返回定时器。实现该行为需要（ ）。

- A. 中断优先级与嵌套现场保存恢复
- B. 关闭所有中断直到定时器结束
- C. 仅使用轮询代替中断
- D. 禁止磁盘中断注册

<!-- ANS A | 中断嵌套 | deep -->

### 66. [medium] 某设备驱动在更新共享状态寄存器时短暂关中断，该临界区设计应遵循的原则是（ ）。

- A. 尽量缩短，仅保护真正临界操作
- B. 关中断期间执行全部业务逻辑
- C. 永久屏蔽低优先级中断
- D. 用关中断替代所有互斥锁

<!-- ANS A | 中断屏蔽 | medium -->

### 67. [deep] 某进程访问序列 1,2,3,4,1,2,5,1,2,3,4,5，物理页框从 3 增至 4 时，FIFO 缺页次数反而增加，该现象称为（ ）。

- A. Belady 异常
- B. 抖动 Thrashing
- C. 写时复制
- D. 内部碎片

<!-- ANS A | 页面置换Belady | deep -->

### 68. [deep] 某 Web 服务器进程内创建 4 个线程并发处理请求。与独立 4 进程相比，它们共享进程（ ），各自拥有独立的（ ）。

- A. 地址空间与打开文件；线程栈与程序计数器
- B. 线程栈；全局堆与代码段
- C. 程序计数器；全部内存空间
- D. 无共享资源；完全隔离

<!-- ANS A | 进程线程 | deep -->

### 69. [deep] 双核 CPU 各自 L1 Cache 存有同一内存地址不同副本，MESI 协议通过总线嗅探保持一致。它解决的是（ ）。

- A. 多核私有 Cache 副本一致性问题
- B. 磁盘坏道修复
- C. 网络 TCP（可靠传输） 拥塞
- D. SQL 事务隔离级别

<!-- ANS A | 多核一致性 | deep -->

### 70. [deep] 多线程分别写不同变量，但两变量映射同一 64B Cache 行，导致行在核间频繁无效化，性能骤降。该现象是（ ）。

- A. 伪共享（false sharing）
- B. 真共享必要同步
- C. Cache 完全命中
- D. TLB 缺失

<!-- ANS A | 伪共享 | deep -->

### 71. [deep] 大型机体系结构中，专门执行 I/O 指令、管理外设与主存数据交换、使 CPU 不必逐字节轮询的部件接近（ ）。

- A. I/O 通道
- B. 算术逻辑单元 ALU
- C. 指令译码器
- D. 分支预测器

<!-- ANS A | 通道 | deep -->

### 72. [deep] 某超标量处理器采用5级流水线与动态分支预测。测试程序含大量不可预测条件跳转，监测发现CPI从理想1.2升至2.8，且后端执行单元频繁空闲。造成这一性能损失的首要机制是（ ）。

- A. 指令Cache未命中导致取指停顿
- B. 预测错误引发流水线冲刷并重新取指
- C. 数据Cache写回带宽不足
- D. TLB缺页中断打断执行流

<!-- ANS B | 分支预测失败 | deep -->

### 73. [deep] 某CPU Cache容量32KB、块大小64B、采用8路组相联。设计团队对比直接映射与全相联后选择当前方案，主要权衡点是（ ）。

- A. 在直接映射冲突Miss与全相联查找复杂度之间折中
- B. 用全相联硬件换取比直接映射更少的总线事务
- C. 以直接映射的简单性完全替代相联比较逻辑
- D. 仅为了支持写直达而必须采用组相联

<!-- ANS A | 组相联 | deep -->

### 74. [deep] 进程访问某虚拟页时触发缺页中断，操作系统从磁盘调入该页、更新页表项有效位与物理帧号后，正确的后续处理是（ ）。

- A. 丢弃引发缺页的那条指令并跳到下一条继续
- B. 回到引发缺页的那条指令重新执行
- C. 立即终止进程并释放全部页帧
- D. 仅更新TLB后从进程入口重启执行

<!-- ANS B | 缺页处理 | deep -->

### 75. [deep] 某嵌入式团队评估是否启用请求分页虚拟存储。下列关于虚拟存储设计目标的叙述中，错误的是（ ）。

- A. 让进程逻辑地址空间大于物理内存
- B. 通过局部性利用提高有效内存利用率
- C. 使程序彻底摆脱缺页与页面置换的运行开销
- D. 简化多进程间的地址隔离与保护

<!-- ANS C | 虚拟存储目的 | deep -->

### 76. [deep] 某用户态程序执行整数除法时除数为 0，CPU 暂停当前指令并向内核报告。该事件在分类上属于（ ），处理路径上通常（ ）。

- A. 异常（内中断）；经异常处理程序可能终止或修复后继续
- B. 外部硬件中断；直接返回用户态忽略
- C. 系统调用；由用户库自行处理
- D. DMA 完成信号；触发磁盘读写

<!-- ANS A | 中断与异常 | deep -->

### 435. [basic] RTOS 中：高优先级任务 H 等待低优先级 L 持有的锁，而中优先级 M 抢占 L 运行，导致 H 被间接长时间阻塞。这称为（ ）。

- A. 优先级反转
- B. 优先级继承
- C. 时间片轮转
- D. 抢占式调度失效

<!-- ANS A | 优先级反转 | basic -->

### 436. [basic] 当高优先级任务因互斥锁被低优先级任务阻塞时，系统将锁持有者临时提升至 H 的优先级，待释放锁后恢复。该机制是（ ）。

- A. 优先级天花板协议
- B. 优先级继承协议
- C. 完全公平调度 CFS
- D. 最早截止时间优先 EDF

<!-- ANS B | 优先级继承 | basic -->

### 437. [basic] 可穿戴设备在屏幕关闭时关闭 GPS 模块、降低 CPU 主频并进入浅睡眠，待按键中断唤醒。这属于（ ）。

- A. 低功耗设计策略
- B. 硬实时截止期保证
- C. 看门狗复位机制
- D. 多核 Cache 一致性

<!-- ANS A | 低功耗设计 | basic -->

### 438. [basic] 工业 PLC 程序固化在专用芯片，内存仅 512KB，需在 10ms 内响应传感器并控制继电器。这体现嵌入式系统的（ ）。

- A. 通用性、资源充裕
- B. 专用性、资源受限与实时性
- C. 仅运行桌面操作系统
- D. 以批处理吞吐为首要目标

<!-- ANS B | 嵌入式特点 | basic -->

### 439. [basic] 车载 ECU 主循环须周期喂狗，若程序跑飞未按时复位计数器，硬件将强制重启 MCU。该部件是（ ）。

- A. DMA 控制器
- B. 硬件看门狗
- C. JTAG 调试接口
- D. UART 串口

<!-- ANS B | 看门狗 | basic -->

### 440. [basic] 汽车 ABS 若制动指令晚于 5ms 送达可能导致失控，系统必须在截止期内完成控制。这属于（ ）。

- A. 软实时（偶尔超时可接受）
- B. 硬实时（超时后果严重）
- C. 交互式分时系统
- D. 离线批处理系统

<!-- ANS B | 硬实时 | basic -->

### 441. [deep] 某工业控制器主循环因死锁停止向看门狗定时器「喂狗」，超过预设超时阈值后系统未响应任何外部指令。在此情况下看门狗的典型行为是（ ）。

- A. 继续等待直至主循环自行恢复
- B. 触发系统复位以恢复运行
- C. 自动切换到备用操作系统
- D. 仅记录日志但不采取硬件动作

<!-- ANS B | 看门狗 | deep -->

### 442. [deep] 某RTOS项目中，低优先级任务L持有互斥锁，高优先级任务H等待该锁，中优先级任务M抢占L导致H被间接阻塞（优先级反转）。下列对策可行的是（ ）。

- A. 降低H的优先级至低于M
- B. 采用优先级继承或优先级天花板协议
- C. 取消所有任务优先级，改为轮询调度
- D. 增大互斥锁持有时间以减少竞争

<!-- ANS B | 优先级反转对策 | deep -->

### 443. [deep] 某野外环境监测终端以电池供电，需持续运行6个月。硬件支持休眠唤醒，软件可控制外设开关与CPU频率。下列软硬件协同降耗手段组合最合理的是（ ）。

- A. CPU全速常开，所有外设持续供电
- B. 休眠唤醒、DVFS降频、外设按需开关、减少无线空口活动
- C. 仅增大电池容量，不做软件优化
- D. 关闭看门狗以减少周期性唤醒

<!-- ANS B | 低功耗 | deep -->

### 444. [deep] 某无人机飞控系统中，舵面控制回路必须在2ms截止期内完成计算与输出，错过可能导致姿态失控甚至坠机。该控制任务在实时性分类上属于（ ）。

- A. 软实时任务
- B. 硬实时任务
- C. 非实时任务
- D. 批处理任务

<!-- ANS B | 硬实时 | deep -->

### 447. [basic] 商户在微信内发布轻量购物页，用户扫码即用、无需安装独立 App，但需遵守平台审核与 API 限制。这属于（ ）。

- A. 独立原生应用商店分发
- B. 超级 App 生态内小程序
- C. 大型机批处理作业
- D. 嵌入式 RTOS 固件

<!-- ANS B | 小程序 | basic -->

### 453. [deep] 某智能硬件厂商需开发配套App：强依赖蓝牙低功耗通信、要求60fps复杂动画、需访问厂商私有SDK。技术选型评审中，下列方案最优先的是（ ）。

- A. 纯H5网页封装为App
- B. 原生开发或原生模块为主
- C. 统一使用跨端框架且不做原生扩展
- D. 仅开发小程序版本

<!-- ANS B | 原生 vs 跨端 | deep -->

### 499. [basic] 智能产线：传感器采集温度→边缘分析决策→PLC 调节阀门开度。该信息流体现 CPS（ ）。

- A. 感知—分析决策—控制执行闭环
- B. 仅离线批处理报表
- C. 单向人工录入 Excel
- D. 纯软件无物理过程

<!-- ANS A | CPS闭环 | basic -->

### 500. [basic] 风电场叶片应力监测与变桨控制由嵌入式控制器与 SCADA 协同，计算与物理过程深度融合。这属于（ ）。

- A. 信息物理系统 CPS
- B. 仅办公 OA 流程
- C. 纯社交媒体平台
- D. 静态网站 CMS

<!-- ANS A | CPS | basic -->

### 503. [deep] 相比普通 MIS 只存业务单据，智能仓储 CPS 用 RFID 感知货位、算法调度 AGV、驱动机械臂拣选。其更强调（ ）。

- A. 感知—决策—控制闭环与实时安全
- B. 仅月末批量对账
- C. 仅员工考勤打卡
- D. 仅邮件审批流程

<!-- ANS A | CPS闭环 | deep -->

### 507. [deep] 某方案A：大量传感器上网汇聚数据至云平台供分析，很少下发控制指令。方案B：感知数据与执行器联动，实时调节产线参数。两方案分别更贴近（ ）。

- A. A偏CPS，B偏IoT
- B. A偏IoT，B偏CPS
- C. 两者均仅为传统MIS
- D. 两者无任何区别

<!-- ANS B | IoT与CPS | deep -->

### 509. [deep] 温控 CPS 中传感器到执行器回路延迟从 20ms 增至 200ms，PID 仍用原参数，最可能出现（ ）。

- A. 振荡、超调或系统失稳
- B. 数据库索引失效
- C. DNS（域名解析） 解析变慢
- D. UI 字体渲染模糊

<!-- ANS A | 反馈延迟 | deep -->

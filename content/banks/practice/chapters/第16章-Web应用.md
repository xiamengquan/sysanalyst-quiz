# 综合知识 · 第16章 Web应用

共 26 题。

---

### 409. [basic] 用户通过浏览器访问集中部署的Web应用，业务逻辑与数据主要在服务器端处理。该结构是（ ）。

- A. B/S（浏览器/服务器）
- B. 传统重型C/S客户端
- C. 单机离线程序
- D. 纯P2P文件共享

<!-- ANS A | B/S结构 | basic -->

### 410. [basic] Web项目中，Controller接收请求并调度，Model封装业务数据，View负责页面呈现。该划分遵循（ ）模式。

- A. MVC（模型-视图-控制器）
- B. FTP文件传输
- C. RAID磁盘阵列
- D. DHCP地址分配

<!-- ANS A | MVC | basic -->

### 411. [basic] 某开放API文档约定：/orders/{id} 用 GET 查询、POST 创建、PUT 全量更新、DELETE 删除，资源以 URI 标识。这最符合哪种架构风格（ ）。

- A. SOAP 远程过程调用
- B. REST 面向资源风格
- C. RPC 仅传方法名调用
- D. GraphQL 单一端点查询

<!-- ANS B | REST | basic -->

### 412. [basic] 电商站点用户评论框若未过滤脚本标签，攻击者注入脚本窃取 Cookie，此类风险属于（ ）。

- A. SQL 注入
- B. 跨站脚本 XSS
- C. 跨站请求伪造 CSRF
- D. 目录遍历

<!-- ANS B | Web安全 | basic -->

### 413. [basic] 登录后服务端生成 SessionID 写入 Set-Cookie，后续请求携带该 Cookie 维持登录态。Cookie 在此主要承担（ ）。

- A. 客户端持久化会话标识
- B. 替代 HTTPS 加密传输
- C. 在 DNS 层解析域名
- D. 压缩 HTTP 响应体

<!-- ANS A | 会话管理 | basic -->

### 414. [basic] 微服务集群对外只暴露一个 HTTPS 域名，由 Nginx 将 /api/* 转发到内网各服务并统一鉴权、限流。该组件角色是（ ）。

- A. 正向代理替客户端上网
- B. 反向代理/API 网关
- C. 数据库主从复制
- D. CDN 静态资源缓存

<!-- ANS B | 反向代理/网关 | basic -->

### 415. [basic] 订单服务高峰时通过 Kubernetes 将 Pod 从 3 个扩到 10 个分担流量，而未升级单机 CPU 规格。这属于（ ）。

- A. 垂直扩展（Scale Up）
- B. 水平扩展（Scale Out）
- C. 数据分片迁移
- D. 单点热备切换

<!-- ANS B | 水平扩展 | basic -->

### 416. [deep] 某电商平台将API网关定位为统一入口，负责路由、鉴权、限流与协议转换。架构评审中，下列能力不宜作为网关核心职责长期承载的是（ ）。

- A. JWT令牌校验与路由转发
- B. 全局限流与熔断降级触发
- C. 订单状态机与库存扣减等领域业务规则
- D. HTTP到内部gRPC的协议适配

<!-- ANS C | 网关职责 | deep -->

### 417. [deep] 某开放API采用REST风格设计，架构师要求服务端不在请求之间保存客户端会话上下文，每个请求须携带完成操作所需的全部信息（如令牌、资源标识）。这体现了REST的哪项约束（ ）。

- A. 无状态（Stateless）
- B. 统一接口（Uniform Interface）
- C. 分层系统（Layered System）
- D. 按需代码（Code on Demand）

<!-- ANS A | REST无状态 | deep -->

### 418. [deep] 某社交App Web层已通过负载均衡水平扩展至20台，但单机MySQL写入QPS已达上限，成为系统瓶颈。下列演进路径最符合常见实践的是（ ）。

- A. 继续增加Web服务器数量
- B. 读写分离、引入缓存、分库分表并处理一致性
- C. 将所有业务逻辑移入Web层减少DB访问
- D. 改用静态HTML页面替代动态查询

<!-- ANS B | 水平扩展瓶颈 | deep -->

### 419. [deep] 某医疗预约平台在排查502错误时，开发人员拟将完整请求日志（含身份证号、手机号、支付卡号）导出至共享网盘供外包分析。从Web应用安全与合规角度，日志记录应避免（ ）。

- A. 记录请求时间戳与HTTP状态码
- B. 明文输出证件号、口令、完整银行卡等敏感信息
- C. 记录TraceId用于链路追踪
- D. 记录接口路径与响应耗时

<!-- ANS B | 日志脱敏 | deep -->

### 420. [deep] 某零售集团同时运营微信小程序与PC Web商城，两者页面结构差异大：小程序需聚合5个后端服务的字段，Web端仅需3个。架构师提议为各前端通道单独部署聚合接口层。该模式通常称为（ ）。

- A. API Gateway（通用网关）
- B. BFF（Backend for Frontend）
- C. ESB（企业服务总线）
- D. Sidecar（边车代理）

<!-- ANS B | BFF | deep -->

### 421. [deep] 某秒杀系统在网关层实施限流以保护下游订单服务。架构师需在「允许一定突发流量」与「输出速率平滑」之间权衡，下列算法组合中常用于限流场景的是（ ）。

- A. RSA、AES、SHA-256
- B. 令牌桶、漏桶、滑动窗口
- C. B树、哈希表、红黑树
- D. 轮询、加权轮询、最小连接

<!-- ANS B | 限流算法 | deep -->

### 422. [deep] 某论坛搜索功能将用户输入的关键词直接拼入响应HTML页面，攻击者构造含<script>标签的URL诱导受害者点击，受害者浏览器执行恶意脚本。该漏洞属于反射型XSS，其根本原因是（ ）。

- A. 数据库未对查询语句参数化
- B. 不可信输入未转义就输出到页面被浏览器当脚本执行
- C. Cookie未设置HttpOnly标志
- D. HTTPS证书配置错误导致中间人攻击

<!-- ANS B | XSS | deep -->

### 423. [deep] 某 Web 应用 Session 仅存 Tomcat 本地内存，负载均衡改为轮询后用户频繁掉线。架构师应优先评估（ ）。

- A. 引入 Redis 集中会话或会话粘滞
- B. 将所有请求固定打到首台机器
- C. 关闭 HTTPS 减少握手开销
- D. 改用 FTP 传输静态页面

<!-- ANS A | 会话与扩展 | deep -->

### 424. [deep] 某支付网关短暂超时后，上千客户端无退避地每秒重试 10 次，导致下游数据库连接池耗尽、故障扩散。该现象称为（ ）。

- A. 重试风暴
- B. 缓存穿透
- C. 脑裂
- D. 单点登录失效

<!-- ANS A | 重试风暴 | deep -->

### 425. [deep] 库存扣减接口要求：客户端因网络抖动重复提交同一订单号，服务端不得多次扣减。按 HTTP 语义，更宜设计为（ ）。

- A. POST 每次创建新资源
- B. PUT 带订单号幂等更新
- C. GET 携带扣减副作用
- D. TRACE 回显请求体

<!-- ANS B | 幂等HTTP | deep -->

### 426. [deep] 某支付App新版本需上线，产品要求先向1%用户放量，观察崩溃率与交易成功率24小时后再逐步扩大至全量。该发布策略的核心机制是（ ）。

- A. 蓝绿两套环境瞬时切换流量
- B. 按比例/名单逐步放量，观察指标再全量
- C. 新旧版本并行运行数周对照验证
- D. 仅在测试环境验证后直接全量替换

<!-- ANS B | 灰度发布 | deep -->

### 427. [deep] 某促销零点流量激增，应用日志频繁出现「获取数据库连接超时」，监控显示连接池100%占用且大量慢查询堆积。下列治理措施组合最合理的是（ ）。

- A. 无限增大连接池上限，不做其他限制
- B. 限流、舱壁隔离、优化慢查询并调整池化参数
- C. 关闭所有数据库索引以加速写入
- D. 将所有查询改为同步阻塞调用延长超时时间

<!-- ANS B | 连接池打满 | deep -->

### 428. [deep] 某商品详情页使用Redis缓存，更新商品价格在MySQL事务提交后偶发缓存与数据库不一致。架构师评估治理方案，下列做法组合最常见有效的是（ ）。

- A. 取消缓存，所有读请求直查数据库
- B. Cache Aside更新策略、合理TTL、失效通知与幂等重试
- C. 只延长缓存TTL至24小时减少更新频率
- D. 写缓存但不写数据库以提升性能

<!-- ANS B | 缓存一致性 | deep -->

### 429. [deep] 某SaaS平台维护蓝（当前生产）与绿（新版本）两套完全相同的运行环境，验证通过后通过负载均衡一键切换流量。相比滚动发布，该方式的主要优点是（ ）。

- A. 硬件资源占用最少
- B. 切换快、回滚快，降低发布风险
- C. 无需维护两套环境即可实现
- D. 适合数据库Schema频繁大变更的场景

<!-- ANS B | 蓝绿部署 | deep -->

### 430. [deep] 某Kubernetes集群中，订单服务某Pod因内存泄漏频繁OOM，但负载均衡仍向其转发请求导致大量500错误。配置健康检查并在探测失败时摘除实例，主要目的是（ ）。

- A. 自动扩容Pod副本数
- B. 避免流量打到故障节点，提高可用性
- C. 替代日志系统完成故障诊断
- D. 强制执行滚动升级策略

<!-- ANS B | 健康检查 | deep -->

### 431. [deep] 某微服务配置中心支持热推送，一次误操作将数据库连接串推至全部生产实例导致大面积故障。下列防护措施组合最有效的是（ ）。

- A. 禁止任何配置变更，全部硬编码
- B. 灰度推送、变更校验、权限控制、审计与快速回滚
- C. 仅允许开发人员直接SSH修改生产配置
- D. 取消配置版本管理以简化流程

<!-- ANS B | 配置热更新风险 | deep -->

### 432. [deep] 某银行网银用户登录后，攻击者诱导其访问恶意页面，该页面自动向银行转账接口提交表单（携带用户Cookie）。下列防御CSRF的有效手段组合是（ ）。

- A. SameSite Cookie、CSRF Token、校验Origin/Referer
- B. 仅对密码字段做MD5哈希
- C. 禁用HTTPS改用HTTP提升速度
- D. 在前端隐藏转账按钮

<!-- ANS A | CSRF防护 | deep -->

### 433. [deep] 某调用链中，网关对入口请求实施每秒1000次上限控制；订单服务对库存服务调用失败率超50%时自动短路返回。这两种机制的主要差别是（ ）。

- A. 限流控流量上限；熔断在失败率高时短开快速失败
- B. 限流用于加密传输；熔断用于解密响应
- C. 两者完全相同，只是命名不同
- D. 限流在失败时触发；熔断在流量高峰时触发

<!-- ANS A | 熔断与限流差别 | deep -->

### 434. [deep] 某新闻站点将JS/CSS/img部署至CDN，用户反馈更新后仍加载旧版资源。前端工程师需确保版本更新后客户端获取新文件，常用策略是（ ）。

- A. 文件名加内容哈希或版本号，配合合理缓存策略
- B. 关闭浏览器所有缓存功能
- C. 将CDN缓存TTL设为永久有效
- D. 每次更新修改HTML域名

<!-- ANS A | 静态资源 | deep -->

---

## 本章答案速查

409-A 410-A 411-B 412-B 413-A 414-B 415-B 416-C 417-A 418-B 419-B 420-B 421-B 422-B 423-A 424-A 425-B 426-B 427-B 428-B 429-B 430-B 431-B 432-A 433-A 434-A

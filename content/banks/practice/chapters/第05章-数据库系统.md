# 综合知识 · 第05章 数据库系统

共 59 题。

---

### 103. [basic] 分区发生后，某分布式数据库选择保证可用性与分区容忍，牺牲强一致，属于 CAP 中（ ）。

- A. CA 系统
- B. AP 取向
- C. CP 且同时满足三者
- D. 单机 ACID 数据库

<!-- ANS B | CAP理论 | basic -->

### 104. [basic] 需求分析阶段用矩形、椭圆、菱形分别表示实体、属性、联系，该模型是（ ）。

- A. E-R 概念模型
- B. 关系模式 3NF
- C. 物理存储 B+ 树
- D. UML 时序图

<!-- ANS A | E-R图 | basic -->

### 105. [basic] 日志系统需高吞吐键值存取，文档型 CMS 需嵌套 JSON，图数据库适合社交关系，它们分属 NoSQL 的（ ）。

- A. 键值、文档、图
- B. 全是关系型 SQL
- C. 仅列族存储
- D. 仅 XML 数据库

<!-- ANS A | NoSQL类型 | basic -->

### 106. [basic] 财务分析需按「时间×地区×产品」多维切片钻取汇总，典型采用（ ）。

- A. OLTP 联机事务处理
- B. OLAP 联机分析处理
- C. 仅全文检索
- D. 批处理磁带备份

<!-- ANS B | OLAP | basic -->

### 107. [basic] DBMS 中用户视图、全局逻辑模式、存储文件结构分别对应三级模式中的（ ）。

- A. 外模式、概念模式、内模式
- B. 内模式、外模式、概念模式
- C. 概念、外、内颠倒
- D. 仅物理模式一层

<!-- ANS A | 三级模式 | basic -->

### 108. [basic] 某银行两柜员事务并发读取账户余额 1000 元，各自加 100 后写回，最终余额 1100 而非 1200。该并发异常属于（ ）。

- A. 脏读
- B. 不可重复读
- C. 丢失更新
- D. 幻读

<!-- ANS C | 丢失更新 | basic -->

### 109. [basic] 将索引从 B 树改为哈希而不改变用户 SQL 视图，体现（ ）。

- A. 物理独立性
- B. 逻辑独立性
- C. 无独立性
- D. 仅应用层缓存

<!-- ANS A | 两级独立性 | basic -->

### 110. [basic] 订单表 `(order_id, user_id, amount)` 中，`order_id` 能唯一标识每一行且非空，它应定义为（ ）。

- A. 主键
- B. 普通可选属性
- C. 外键引用自身
- D. 仅索引非唯一

<!-- ANS A | 主键 | basic -->

### 111. [basic] 转账事务要么 debit/credit 同时成功要么全部回滚，提交后即使断电也不丢失，分别对应 ACID 的（ ）。

- A. 原子性与持久性
- B. 仅隔离性
- C. 仅一致性
- D. 弹性与可用性

<!-- ANS A | 事务ACID | basic -->

### 112. [basic] 关系模式 Student(Sno, Sname, IdCard, Dept) 中，Sno 与 IdCard 均能唯一标识学生且均不可再删属性。该模式的一个候选键是（ ）。

- A. Sno（英文缩写，见解析中文说明）
- B. Sname（英文缩写，见解析中文说明）
- C. Dept（英文缩写，见解析中文说明）
- D. Sname, Dept（英文缩写，见解析中文说明）

<!-- ANS A | 候选键 | basic -->

### 113. [basic] 订单表 Order 的外键 CustomerId 引用客户表 Customer 的主键。插入一条 Order 记录时，下列做法符合参照完整性的是（ ）。

- A. CustomerId 取 Customer 中已存在的主键值
- B. CustomerId 取任意未在 Customer 出现的值
- C. CustomerId 必须为空且不允许非空
- D. CustomerId 可引用 Customer 的非主键列任意值

<!-- ANS A | 参照完整性 | basic -->

### 114. [basic] 表 Employee(EmpNo PK, Name, Salary) 执行 INSERT 时，下列哪种情况违反实体完整性约束（ ）。

- A. EmpNo 插入 NULL
- B. Name 插入 NULL
- C. Salary 插入 0
- D. EmpNo 插入已存在的主键值

<!-- ANS A | 实体完整性 | basic -->

### 115. [basic] 两事务并发更新同一账户余额，未加锁时可能出现丢失更新。采用封锁协议的主要目的是（ ）。

- A. 保证事务 ACID 中的并发控制，避免脏读/不可重复读/丢失更新
- B. 替代日志实现持久性
- C. 自动消除所有死锁且无需等待
- D. 将关系模式规范化到 3NF

<!-- ANS A | 封锁协议 | basic -->

### 116. [basic] 企业建设数据仓库用于历年销售主题分析，数据来自 ERP、CRM 等多源并做清洗集成，历史快照保留不变。这体现了数据仓库的（ ）特征。

- A. 面向主题、集成、相对稳定、反映历史变化
- B. 面向事务处理、实时更新、高度规范化
- C. 仅存储当前快照、不支持多维分析
- D. 与 OLTP 共用同一套 3NF 表结构

<!-- ANS A | 数据仓库 | basic -->

### 117. [basic] 数据库系统在 CHECKPOINT 之后发生故障，恢复时需要重做已提交事务、撤销未完成事务。该过程主要依赖（ ）。

- A. 事务日志（redo/undo 信息）
- B. 仅数据文件副本无需日志
- C. 外键约束自动回滚
- D. 视图定义

<!-- ANS A | 日志与恢复 | basic -->

### 118. [basic] 表 Contact 有一列 Address 存储“北京市/海淀区/中关村大街1号”的复合字符串。要满足第一范式，最合适的调整是（ ）。

- A. 拆分为省、市、详细地址等原子属性
- B. 改为 BLOB 存储即可
- C. 增加一行重复存储同一地址
- D. 删除 Address 列

<!-- ANS A | 第一范式1NF | basic -->

### 119. [basic] 关系 R(A,B,C,D)，候选键 AB，已知 B→C，C→D，且 D 不传递依赖于候选键的组成部分。为消除传递依赖，R 至少应达到（ ）。

- A. 第三范式 3NF
- B. 第一范式 1NF
- C. 仅第二范式 2NF 即可
- D. 无需分解

<!-- ANS A | 第三范式3NF | basic -->

### 120. [basic] 选课表 SC(Sno, Cno, Grade, Cname)，主键 (Sno,Cno)，已知 Cno→Cname。该模式主要违反（ ）。

- A. 2NF：Cname 部分依赖于候选键（仅依赖 Cno）
- B. 1NF：Grade 非原子
- C. 3NF：无传递依赖
- D. BCNF：所有决定因素都是候选键

<!-- ANS A | 第二范式2NF | basic -->

### 121. [basic] 某 HR 系统对普通员工只开放视图 V_EmpPublic，隐藏 Salary 列；管理员直接访问基表。视图在此场景中的主要作用是（ ）。

- A. 逻辑独立与权限控制（虚表屏蔽敏感列）
- B. 物理复制数据以提高写入速度
- C. 替代主键保证实体完整性
- D. 自动将 OLTP 转为数据仓库

<!-- ANS A | 视图 | basic -->

### 122. [deep] 某分布式账户系统在分区讨论CAP时强调：任一成功写操作后，所有副本在同一逻辑时刻读到相同余额。这里的Consistency通常指（ ）。

- A. 仅保证单节点本地事务ACID中的隔离性
- B. 所有节点同一时刻看到相同数据副本（强一致/线性一致语境）
- C. 系统永远可用且永不丢请求
- D. 仅保证最终若干小时后数据收敛

<!-- ANS B | CAP中的C | deep -->

### 123. [deep] 某OLTP数据库采用MVCC，读事务不阻塞写事务、写事务也不阻塞普通读。其实现读一致快照的关键机制是（ ）。

- A. 对所有表加表级排他锁直到事务结束
- B. 维护多版本数据，读操作访问事务开始时的版本快照
- C. 强制所有读写走单线程队列
- D. 读前必须刷新全部数据到磁盘

<!-- ANS B | MVCC | deep -->

### 124. [deep] 事务隔离级别设为Read Committed。事务T1两次读取同一范围数据，期间T2插入并提交新行，T2已提交的数据T1仍可能（ ）。

- A. 完全看不到任何并发变更
- B. 出现不可重复读或幻读
- C. 自动升级为串行化执行
- D. 只能读到未提交的脏数据

<!-- ANS B | 读已提交现象 | deep -->

### 125. [deep] 并发调度中所有事务均严格遵循两段锁协议（2PL）：加锁阶段只加锁不解锁，解锁阶段只解锁不加锁。在该前提下可保证（ ）。

- A. 不会发生任何死锁
- B. 调度结果冲突可串行化
- C. 所有事务均满足可重复读隔离
- D. 无需日志即可崩溃恢复

<!-- ANS B | 两段锁 | deep -->

### 126. [deep] 员工表EMP(EmpID, Name)定义EmpID为主键。插入(EmpID=NULL, '张三')时DBMS应拒绝，这违反了（ ）。

- A. 实体完整性
- B. 参照完整性
- C. 用户自定义完整性
- D. 域完整性中的唯一约束

<!-- ANS A | 实体完整性 | deep -->

### 127. [deep] 某电商库存 qty 初值 10。T1 读 qty=10 后加 1 写回；几乎同时 T2 也读 qty=10 加 1 写回；最终 qty=11 而非 12。较合适的预防手段是（ ）。

- A. 乐观锁或 SELECT FOR UPDATE 等并发控制
- B. 降低隔离级别为 READ UNCOMMITTED
- C. 禁止所有读操作
- D. 仅增加索引不加锁

<!-- ANS A | 丢失更新 | deep -->

### 128. [deep] 优化器为SELECT * FROM A JOIN B JOIN C生成执行计划，三表基数与选择率差异显著。决定多表连接先后顺序的主要依据是（ ）。

- A. 表名字母序
- B. 基于统计信息的代价估计（基数、选择性等）
- C. 总是按用户书写顺序连接
- D. 随机选择以避免陷入局部最优

<!-- ANS B | 连接顺序 | deep -->

### 129. [deep] 关系R(学号,课程号,成绩,教师工号)，已知(学号,课程号)→成绩，课程号→教师工号。该关系最高满足（ ）。

- A. 已经是BCNF
- B. 不满足1NF
- C. 1NF，但不满足2NF（成绩对主键完全依赖，但若主键为(学号,课程号)则教师工号部分依赖于课程号）
- D. 一定是3NF

<!-- ANS C | 函数依赖与范式 | deep -->

### 130. [deep] 关系R(Sno, IdCard, Email)中(Sno)、(IdCard)、(Email)均可唯一标识元组。DBA选定Sno作为主键，正确理解是（ ）。

- A. 主键是候选键之一，从多个候选键中选定
- B. 主键与候选键是完全不同的概念不可重叠
- C. 候选键只能有一个，其余是唯一索引
- D. 主键必须包含全部属性

<!-- ANS A | 主键与候选键 | deep -->

### 131. [deep] 某电商平台订单库单表 20 亿行，按 user_id 分 256 库；80% 查询按 user_id+order_time 范围查个人订单，5% 按 order_id 全局查，促销期需扩容。架构师选分片键的首要考虑是（ ）。

- A. 数据均匀度、查询模式匹配、事务边界与扩容能力
- B. 仅选字母序最小的列
- C. 选更新最少的列且忽略跨分片事务
- D. 固定单库避免任何路由

<!-- ANS A | 分库键 | deep -->

### 132. [deep] 订单表ORDER(CustID)引用客户表CUSTOMER(CustID)，参照完整性允许CustID为NULL。某订单CustID=NULL的语义通常是（ ）。

- A. 该订单必须关联到虚拟客户0号
- B. 该订单当前不参照任何客户记录（可选关系）
- C. 违反参照完整性应立即回滚
- D. 表示客户表被级联删除

<!-- ANS B | 外键为空 | deep -->

### 133. [deep] 事务T在Repeatable Read下第一次执行SELECT COUNT(*) WHERE status='待审'得5；提交前再次同样查询得7，因其他事务插入了新行。该现象称为（ ）。

- A. 脏读
- B. 不可重复读
- C. 幻读
- D. 丢失更新

<!-- ANS C | 幻读 | deep -->

### 134. [deep] 某 DBA 将行级锁改为表级锁以简化实现。与改前相比，预期变化是：并发度通常（ ），锁管理开销通常（ ）。

- A. 更高；更大
- B. 更低；更小
- C. 更高；更小
- D. 更低；更大

<!-- ANS B | 封锁粒度 | deep -->

### 135. [deep] 某零售集团OLTP库支撑收银，新建数据仓库支撑三年销售趋势分析。相对OLTP，数据仓库更强调（ ）。

- A. 高并发短事务与实时更新
- B. 面向主题的历史数据集成与批量分析
- C. 第三范式极致规范化减少冗余
- D. 单行热点更新的锁优化

<!-- ANS B | 数据仓库 vs OLTP | deep -->

### 136. [deep] 某数据库在脏页写磁盘前崩溃，重启后需恢复到一致状态。依赖预写日志（WAL）能恢复的根本原因是（ ）。

- A. 日志可 Redo 已提交事务、Undo 未完成事务，重建一致性
- B. 日志直接替代全部数据文件
- C. WAL 禁止任何并发写入
- D. 仅记录 SELECT 语句

<!-- ANS A | 日志作用 | deep -->

### 137. [deep] 某系统周期性执行 checkpoint，将缓冲池脏页与日志控制信息落盘。与无 checkpoint 相比，该机制主要有助于（ ）。

- A. 缩短故障恢复时需要回放的日志长度
- B. 完全取消事务日志
- C. 提高 SQL 语法检查速度
- D. 自动消除所有死锁

<!-- ANS A | 检查点 | deep -->

### 138. [deep] 关系模式R(A,B,C,D)，候选键AB，存在函数依赖A→C、C→D（C非键）。为消除非主属性对候选键的传递依赖，应分解至（ ）。

- A. 1NF（英文缩写，见解析中文说明）
- B. 2NF（英文缩写，见解析中文说明）
- C. 3NF（英文缩写，见解析中文说明）
- D. BCNF（英文缩写，见解析中文说明）

<!-- ANS C | 3NF | deep -->

### 139. [deep] 某企业对外提供 employee_public 视图，仅含工号与部门，隐藏 salary 列及基表结构。与直接授权基表相比，主要收益是（ ）。

- A. 逻辑独立性与细粒度访问控制
- B. 物理存储空间减半
- C. 自动提升查询并行度
- D. 替代主键约束

<!-- ANS A | 视图用途 | deep -->

### 140. [deep] 若要避免脏读，但允许不可重复读与幻读，通常对应的隔离级别是（ ）。

- A. 可串行化
- B. 无隔离
- C. 读已提交（Read Committed）
- D. 读未提交

<!-- ANS C | 隔离级别 | deep -->

### 141. [deep] 电商采用主库写、从库读。用户刚下单支付成功，立即查订单却显示未支付。最可能原因是（ ）。

- A. 主库未提交事务
- B. 主从异步复制存在延迟，从库尚未同步
- C. 从库只读导致写失败
- D. DNS（域名解析） TTL过长

<!-- ANS B | 读写分离延迟 | deep -->

### 142. [deep] 某报表系统对星型宽表查询极频繁，DBA 考虑将高度规范化的 10 表连接改为适度冗余。过度规范化在该场景的主要风险是（ ）。

- A. 多表连接增加查询开销，分析性能下降
- B. 必然导致数据丢失
- C. 违反第一范式
- D. 无法建立任何索引

<!-- ANS A | 规范化代价 | deep -->

### 143. [deep] 批处理程序用游标逐行更新 500 万行且全程未 COMMIT，数据库为 MVCC 引擎。最可能出现的并发问题是（ ）。

- A. 长事务持锁/版本链过长，阻塞其他会话并堆积旧版本
- B. 游标自动将表升级为只读
- C. MVCC 下长事务不会占用任何资源
- D. 仅增加网络带宽消耗，不改变锁与版本状态

<!-- ANS A | 游标与并发 | deep -->

### 144. [deep] 某秒杀活动大量并发 UPDATE 同一库存行，出现严重锁等待。较合理的优化思路包括（ ）。

- A. 分段库存、队列削峰、乐观锁重试
- B. 关闭事务保证
- C. 将所有 UPDATE 改为全表扫描
- D. 删除库存字段改用内存变量

<!-- ANS A | 热点行 | deep -->

### 462. [basic] 日志平台将 500GB 文件切分为 128MB 块，在集群多节点各存 3 副本以容错。该存储思想源自（ ）。

- A. HDFS 大文件分块多副本
- B. 单机 ext4 日志结构
- C. 关系型 B+ 树索引
- D. 内存 Redis 键值

<!-- ANS A | HDFS思想 | basic -->

### 463. [basic] 实时大屏用 Storm 算近 5 分钟指标，历史报表用 Spark 批处理全量，查询层合并两层结果。该架构是（ ）。

- A. Kappa 仅流处理
- B. Lambda 批流分层
- C. 单体三层 MVC（模型-视图-控制器）
- D. 星型维度建模

<!-- ANS B | Lambda架构 | basic -->

### 464. [basic] 词频统计作业：各节点 Map 输出 (word,1)，Shuffle 后 Reduce 按 key 求和。该计算模型是（ ）。

- A. MapReduce 映射归约
- B. ACID 两阶段提交
- C. BSP 超步同步
- D. Actor 消息邮箱

<!-- ANS A | MapReduce | basic -->

### 465. [basic] 某平台日增 10TB 日志、毫秒级写入、含文本/图像/JSON 混合格式，需挖掘商业价值。这对应大数据（ ）。

- A. Volume 与 Variety 等 4V 特征
- B. 仅 OLTP 单行事务
- C. 仅 3NF 规范化
- D. 仅单机内存排序

<!-- ANS A | 大数据特征4V | basic -->

### 466. [basic] 企业把传感器、业务库导出、日志以 Parquet/JSON 原格式存入对象存储，待后续 Schema-on-Read 分析。该存储形态是（ ）。

- A. 数据仓库强 Schema 入库
- B. 数据湖原始多源存储
- C. 仅内存缓存层
- D. 区块链账本

<!-- ANS B | 数据湖 | basic -->

### 467. [deep] 某日志分析平台基于HDFS存储PB级数据，默认配置3副本分布在不同机架。该多副本策略的主要目的是（ ）。

- A. 加速单条记录的OLTP事务提交
- B. 容错与数据可靠性，兼具读扩展能力
- C. 替代NameNode的元数据管理
- D. 实现实时流处理的毫秒级延迟

<!-- ANS B | HDFS副本 | deep -->

### 468. [deep] 某广告平台采用Lambda架构：批处理层每日产出精确报表，但业务方还需分钟级曝光统计。速度层（Speed Layer）在该架构中的作用是（ ）。

- A. 替代批处理层成为唯一数据源
- B. 补偿批处理延迟，提供近实时视图
- C. 仅负责数据清洗与ETL入库
- D. 存储冷数据以降低存储成本

<!-- ANS B | 速度层 | deep -->

### 470. [deep] 某集团建设企业级数据湖，各部门未经审批即可将原始数据灌入同一存储。运行两年后分析师反馈「找不到可信数据、口径冲突频发」。该现象最准确的风险描述是（ ）。

- A. 数据湖容量不足导致写入失败
- B. 缺乏治理使数据湖退化为数据沼泽
- C. 批处理速度过快导致实时层失效
- D. 副本数过少导致数据丢失

<!-- ANS B | 数据湖风险 | deep -->

### 472. [deep] 某团队考虑用经典MapReduce处理两类 workload：（甲）PB级日志日批统计；（乙）用户点击后200ms内返回个性化推荐。MapReduce批处理框架不擅长的是（ ）。

- A. 甲：大规模离线聚合
- B. 乙：低延迟交互式查询与小更新频繁的OLTP
- C. 甲：分布式排序
- D. 甲：词频统计

<!-- ANS B | MapReduce局限 | deep -->

### 474. [basic] 下单需同时扣库存、创建订单、冻结积分，跨三个微服务各自数据库。直接两阶段提交往往因性能与可用性改用（ ）。

- A. 本地单库事务即可
- B. 最终一致与 Saga 补偿
- C. 停止其中一个服务
- D. 合并为一个巨型单体库

<!-- ANS B | 分布式事务难点 | basic -->

### 480. [deep] 某旅行预订系统含订单、机票、酒店三个微服务，各服务本地事务独立，无法使用全局2PC。架构师选用Saga模式保证跨服务一致性，其核心思想是（ ）。

- A. 所有服务共享同一数据库锁
- B. 一串本地事务加补偿事务，达到最终一致
- C. 禁止任何跨服务调用
- D. 每次请求强制同步双写所有服务

<!-- ANS B | Saga | deep -->

### 486. [deep] 某电商库存服务在库存变更后发布「StockChanged」领域事件，搜索索引、推荐引擎、BI报表各自订阅处理。相比库存服务直接同步调用所有下游，该方式的主要好处是（ ）。

- A. 强制所有下游强一致实时同步
- B. 解耦发布方与订阅方，易扩展新消费者，支撑最终一致
- C. 消除对消息中间件的依赖
- D. 保证所有订阅方在同一毫秒完成更新

<!-- ANS B | 事件驱动 | deep -->

### 491. [deep] 某团队微服务拆分出现两种极端：方案甲将「注册—下单—支付—报表」全塞入一个服务；方案乙为每张数据库表单独建服务。按DDD限界上下文原则，更合理的拆分策略是（ ）。

- A. 方案甲：越大越好，减少网络调用
- B. 方案乙：一表一服务最解耦
- C. 按业务能力/限界上下文拆分，避免过大过碎
- D. 随机按开发人员数量均分模块

<!-- ANS C | 服务拆分粒度 | deep -->

### 497. [deep] 某电商订单服务与库存服务分属不同数据库，下单需扣减库存且保证尽量可靠。架构师评估务实方案，下列较合理的是（ ）。

- A. 强依赖跨库2PC并锁定所有分片
- B. 本地事务+可靠消息/Outbox、幂等与补偿实现最终一致
- C. 先扣库存后异步补订单，失败不回滚
- D. 合并两库为单库后禁止任何拆分

<!-- ANS B | 分布式事务 | deep -->

### 498. [deep] 某社交Feed采用最终一致性：发帖后好友时间线可能延迟数秒出现。产品与体验设计上常需（ ）。

- A. 向用户隐瞒任何延迟可能性
- B. 明确一致窗口、补偿机制与用户提示，避免假象强一致
- C. 强制所有读请求阻塞至全球同步完成
- D. 取消异步复制改同步双写所有节点

<!-- ANS B | 最终一致 | deep -->

### 561. [basic] 某订票系统在同一事务中扣减库存并生成订单。若扣库存成功但写订单失败，系统回滚使库存恢复。该设计主要保障了事务的（ ）特性。

- A. 原子性（全做或全不做）
- B. 仅界面美观
- C. 仅字体加粗
- D. 取消日志

<!-- ANS A | 事务ACID | basic -->

### 562. [medium] 关系模式中，非主属性完全依赖候选键，但不存在传递依赖，则该模式至少满足（ ）。

- A. 第二范式（2NF）但仍可能未到 3NF
- B. 必然已经是 BCNF 且无任何异常
- C. 仅 1NF 且允许部分依赖
- D. 与范式无关

<!-- ANS A | 范式 | medium -->

# 工坊归档（不参与 sync）

`重写-分片*.jsonl`、`重写-正式v1-全部.jsonl` 等为 **2026-09 重写流水线历史切片**，正文已并入 `content/banks/practice/all.jsonl`（730 题）。

- **勿**放回 `workshop/new/`，否则可能被误同步。
- 新题请走 `workshop/new/YYYYMMDD-批次*-passed.jsonl` 流程。

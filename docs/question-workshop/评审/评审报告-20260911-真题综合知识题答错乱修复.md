# 评审报告：真题综合知识「题目与答案错乱」修复（2026-09-11）

## 结论

`content/banks/real/上午真题.jsonl` 存在可复现的题干/选项/答案错位，已做数据修复并同步至 `public/data`。

**修复前 581 题 → 修复后 567 题**（剔除不可用 14 题；多空题按空号重挂选项约 90 题）。  
站点数据已 `npm run sync:data`：`public/data/questions.json` 中 `real: 567`。

## 问题类型

| 类型 | 表现 | 处理 |
|------|------|------|
| 2025 回忆版孤儿选项行 | 题干仅为 `A. … B. … C. … D. …`，无设问；多题共用 `qnum=1/2` 且答案键整齐成批 | **删除 10 题** |
| 多空共干选项串位 | 如 2019（6）（7）：第 7 空仍挂第 6 空的 AES/RSA 选项，选 D 会变成选 DES 而非「乙的私钥」 | **按空号重解析选项** |
| 选项 A 被吃空 | 表格题路径最短等：答案为 A，但 `opts.A=""`，界面无法正确判定 | **从题干 ABCD 块回填**（如 2018-58、2019-53/56、2020-57、2021-55/57） |
| 选项截断/粘连 | 如 2019-42/43 选项粘到下一空；2023-56 的 A 仅剩「选择部门A.」 | **按空号重切选项** |
| 英文残缺题 | 专项 3 道同残缺 stem「A variety of tools…」却答案互异；2021-73 stem 截断 | **删除 4 题** |

## 未改动的部分

- 2018–2023 正式卷中，多空题**共用同一组选项**属正常命题形态，未当作错乱。
- 顿号/逗号/点号书写差异不属于题答错位。
- 大量正式卷仍无解析（`exp` 空），不影响「选对选错」判定，本次未补解析正文。

## 备份

修复前完整备份：`content/banks/real/上午真题.jsonl.bak-before-qa-fix`（本地备份，可不入库）。

## 建议

1. 2025 回忆版仅作参考刷题；缺题干/无权威答案键的条目不要再导入。
2. 后续若从 PDF/OCR 再入库，解析器须按 `（N）A.…D.…` **按空切选项**，禁止把上一空选项复制到下一空。
3. 若有权威答案册，可对「无解析」正式卷做答案键 spot-check（本次以结构错乱为主，未做全书答案对拍）。

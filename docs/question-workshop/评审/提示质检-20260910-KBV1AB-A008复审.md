# 提示质检复审 · WS-KBV1A-008（2026-09-10）

## 出题者重写

| 项 | 原题问题 | 重写要点 |
|----|----------|----------|
| 函数依赖 | `学号→教师` 会使 `{学号}` 成码，与题设 `{学号,课程号}` 冲突 | 改为 `(学号,课程号)→教师`，`教师→课程号` |
| 答案 | 宣称 3NF，但在错误前提下自洽性不足 | 仍为 **C 3NF**（标准「3NF 非 BCNF」构型） |
| 解析 | 谈传递依赖却未对齐码 | 点明：决定因素非码 → 非 BCNF；右侧主属性 → 仍 3NF |

## 提示信息官复核

| 维度 | 结论 |
|------|------|
| 简洁 | 通过（约 58 字） |
| 合理 | 通过（与精炼「BCNF=每个决定因素都是候选码」一致；R09 未触碰错误定义） |
| 通俗 | 通过（对因 + 易误选 D + A/B 偏低） |
| 对/错场景 | 通过 |

**结论：通过** → 已重新写入 `批次A-passed.jsonl`。

## 入库

重新执行 `merge_to_quiz.py` 后，workshop 应恢复含本试题。

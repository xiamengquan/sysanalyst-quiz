# 案例分析真题 · 领域标签校正（2026-09-25）

- 校正条数：**36** / 59（相对发版前 `all.jsonl`）
- 规则：`scripts/python/case_domain_lib.py`（「阅读以下关于…」优先；题干「核心需求」不再误标需求工程；小题关键词加权）
- 维护脚本：`python3 scripts/python/fix_case_domains.py`（重写 `seven_steps` 第三步领域提示）

## 典型误标（已修复）

| 题 ID | 原标签 | 现标签 | 说明 |
|--------|--------|--------|------|
| `ZT-2026上-案例04` | 需求 | **数据库** | 分片 / Redis 穿透 / 高可用 |
| `ZT-2016上-案例04` | 需求 | **数据库** | 卷面标题即数据库设计 |
| `ZT-2023上-案例04` | 需求 | **数据库** | NoSQL / SNS |
| `ZT-2025下-案例02` | Web | **数据库** | 大促缓存击穿、DB 瓶颈 |

## 完整变更清单

- `ZT-2014上-案例02`：Web·ch16 → **需求·ch11**
- `ZT-2014上-案例05`：需求·ch11 → **Web·ch16**
- `ZT-2015上-案例01`：需求·ch11 → **测试·ch14**
- `ZT-2015上-案例02`：Web·ch16 → **需求·ch11**
- `ZT-2015上-案例03`：需求·ch11 → **嵌入式·ch17**
- `ZT-2015上-案例04`：Web·ch16 → **集成·ch12**
- `ZT-2016上-案例01`：数据库·ch5 → **需求·ch11**
- `ZT-2016上-案例02`：需求·ch11 → **Web·ch16**
- `ZT-2016上-案例03`：需求·ch11 → **嵌入式·ch17**
- `ZT-2016上-案例04`：需求·ch11 → **数据库·ch5**
- `ZT-2016上-案例05`：需求·ch11 → **Web·ch16**
- `ZT-2017上-案例01`：需求·ch11 → **微服务·ch20**
- `ZT-2017上-案例03`：需求·ch11 → **嵌入式·ch17**
- `ZT-2017上-案例04`：需求·ch11 → **数据库·ch5**
- `ZT-2018上-案例04`：需求·ch11 → **数据库·ch5**
- `ZT-2019上-案例02`：需求·ch11 → **架构·ch12**
- `ZT-2019上-案例05`：需求·ch11 → **Web·ch16**
- `ZT-2020上-案例03`：需求·ch11 → **Web·ch16**
- `ZT-2020上-案例05`：需求·ch11 → **Web·ch16**
- `ZT-2021上-案例02`：需求·ch11 → **Web·ch16**
- `ZT-2021上-案例03`：需求·ch11 → **数据库·ch5**
- `ZT-2021上-案例04`：需求·ch11 → **Web·ch16**
- `ZT-2022上-案例01`：架构·ch12 → **需求·ch11**
- `ZT-2022上-案例02`：架构·ch12 → **Web·ch16**
- `ZT-2022上-案例03`：微服务·ch20 → **数据库·ch5**
- `ZT-2023上-案例01`：架构·ch12 → **Web·ch16**
- `ZT-2023上-案例03`：需求·ch11 → **嵌入式·ch17**
- `ZT-2023上-案例04`：需求·ch11 → **数据库·ch5**
- `ZT-2023上-案例05`：需求·ch11 → **数据库·ch5**
- `ZT-2025上-案例01`：需求·ch11 → **Web·ch16**
- `ZT-2025上-案例02`：需求·ch11 → **Web·ch16**
- `ZT-2025下-案例02`：Web·ch16 → **数据库·ch5**
- `ZT-2026上-案例02`：需求·ch11 → **Web·ch16**
- `ZT-2026上-案例03`：需求·ch11 → **嵌入式·ch17**
- `ZT-2026上-案例04`：需求·ch11 → **数据库·ch5**
- `ZT-2026上-案例05`：Web·ch16 → **需求·ch11**

## 自编案例库

`content/banks/cases/all.jsonl`（130 道）领域与文件名「XX · 方案对比」一致，**未改**；若需按题干二次校正可另开任务。

## 综合知识真题

上午选择题无 `domain` 筛选项，按章节/考点筛选；本次未动。

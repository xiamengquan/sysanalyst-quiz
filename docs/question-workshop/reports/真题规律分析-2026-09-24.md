# 真题规律分析（2026-09-24）

> **角色**：分析师（sysanalyst-analyst）主笔；审计乙（规律与证据）复核口径。  
> **数据**：`content/banks/real/综合知识/all.jsonl`（机器可读统计见 `data/真题规律统计-2026-09-24.json`）。  
> **边界**：规律仅来自真题行；自编题仅作差距对照，**不得**并入占比表。

## 1. 执行摘要（≤10 条）

1. 仓库综合知识真题 **n=1040**（2014 上—2026 上），为当前规律统计**唯一权威样本**（取代旧镜像 `上午真题.jsonl` 的 581 条口径）。
2. **全库记录级**题干中位数 **70 字**、均值 **140 字**；**2018–2023 正式六届**（n=450，75 题/届）中位数 **106 字**、均值 **186 字**——引用均长/长短题占比时必须标明「全库」或「正式卷」。
3. **机考短题**（题干 ≤50 字）：全库 **38.1%**；正式卷 **24.4%**——近年卷（2024–2026）拉高全库短题占比，自编库须保留 **short 轨 ≥20%**（§4）。
4. **空位标记**（`（n）` / `(n)`）：全库 **58.1%**；正式卷 **66.9%**——仍属主流设问，但已非旧口径「100% 多空联动」；新题应**混排**独立短题 + 情景长题 + 少量多空联动模仿。
5. **叙述情景**（关键词口径，且 len>40）：全库 **38.8%**；正式卷 **52.7%**——**不得**与「len≥142 长题干记录」（全库 24.2% / 正式 34.4%）混称为「情景占比」。
6. **计算型**关键词：全库 **19.2%**；正式卷 **21.8%**。 **否定型**：全库 **8.7%**。 **对比型**：约 **1%**（仍以定义/辨析为主，对比题靠精炼速查表支撑）。
7. **模板句**（「关于…下列说法/以下…正确的是」且无「某/项目」）：全库约 **6%**——可模仿结构，但自编库须 **≤30% 批次模板占比**（§9）。
8. 案例/论文：本报告未重跑下午卷统计；下午结构仍依 `真题/_raw_text/` 抽样 + 既有细则 §3.2–§3.3。
9. **可模仿**：短中长混排、情景决策、计算/否定比例、干扰项层级错配、空位联动（适度）。**不可照抄**：真题原文、回忆卷当唯一风格、把自编计入规律表。
10. 非真题 **730 道**经 **meta-v1** 重标 `style_track` 后：short **404** / scenario **246** / mid **80**；**119 道**解析仍 &lt;35 字，须 Phase B 由出题者重写。

## 2. 样本与路径

| 口径 | n | 说明 |
|------|---|------|
| 综合知识全库 | 1040 | `content/banks/real/综合知识/all.jsonl` |
| 正式卷 2018–2023 | 450 | year ∈ {2018…2023}，每届 75 |
| 自编练习 | 730 | `content/banks/practice/all.jsonl` |
| 工坊并入 | 见 sync | `content/workshop/new/*-passed.jsonl` 等，bank=workshop |

## 3. 指标操作定义（与 JSON 一致）

| 指标 | 操作定义 |
|------|----------|
| shortExam | len(stem) ≤ 50 |
| longStem | len(stem) ≥ 142 |
| blank | stem 含 `（数字）` 或 `(数字)` |
| scenario | 匹配情景关键词且 len>40 |
| calc / negate / compare | 见 `scripts/analyze-real-patterns.mjs` 关键词表 |

## 4. 对《出题细则报告》的修订建议

- §2.1 样本量改为 **1040 / 450** 与仓库路径。  
- §2.2 拆成 **全库** 与 **正式卷** 两列，废止「431/581」旧计数（除非注明历史文档）。  
- §10 配额：scenario 轨目标向正式卷 **~50% 情景关键词**靠拢，但机考 short 轨不可低于 **20%**。  
- 送审：**细则审计委员会** 对 v1.2 数据刷新表决后，方可标「正式发布」。

## 5. 下一批命题侧重（分析师建议）

| 章节域 | 建议 |
|--------|------|
| 5/7/9/10/12/14 | 维持 boost；补 **scenario + calc** 双标签题 |
| 11 需求工程 | 119 题短解析集中区，优先 **Phase B 解析+情景题干** |
| 3/4 网络/数据库 | 增加 **calc**（掩码、子网、范式）与 **negate** |
| 0/1/2 基础 | 稳定 **short** 机考短题刷题感 |

---

*生成：`node scripts/analyze-real-patterns.mjs`*

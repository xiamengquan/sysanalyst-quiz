# 审计甲 · 结构与覆盖 · API 考点 v1.2 意见

| 字段 | 内容 |
|------|------|
| **审计员** | 审计甲（结构与覆盖官） |
| **审计日期** | 2026-09-26 |
| **送审** | `编制委员会/草稿/编制完成-送审-API考点精修-v1.2.md` |
| **依据** | `审计委员会/00-审计委员会章程.md`、`程序/知识点API参考模型.md`、`content/kb-index.json` |

---

## 核查表

| # | 核查项 | 结论 |
|---|--------|------|
| A1 | `api-ref` 条目数 = `meta.apiPoints`（174） | **通过** |
| A2 | 每篇 `points/kp-*.md` 物理存在且含固定六节 | **通过**（脚本 0 缺失） |
| A3 | `outlineRef` / `group` / `chapter` 与《大纲考点对照》一致 | **通过**（抽查 1.x→第3章、科目2→16—21章） |
| A4 | 主入口 `primaryCatalog: api-ref` 与知识地图说明一致 | **通过** |
| A5 | 篇章合订 `kind: legacy` 归档，与 API 并行不删库 | **通过**（22 章） |
| A6 | `kp-2-7` 手工试点独立于批量精修 | **通过** |
| A7 | 索引 `path` 与 `public/kb` sync 后静态路由 | **通过**（build 218 篇含 points） |

---

## 表决

**结论**：通过

**一票否决项**：无

## 必须修改（阻塞）

无

## 建议修改（不阻塞）

1. 科目3「论文专题 / 论文专题-N」标题与大纲表头同名，目录上宜在 `kb-index` 加 `displayTitle` 或后续改 `title` 为组名（如「数据库建模论文组」）。  
2. `legacy` 篇索引 note 已提示走 API，可在地图 §二增加「API 174 篇」锚点链接。

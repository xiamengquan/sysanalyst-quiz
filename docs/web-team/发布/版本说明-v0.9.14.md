# 版本说明 v0.9.14

**日期**：2026-09-26

## 变更

- 一次性迁移：`migrate_kb_legacy_to_points.py` 将分章精炼正文按大纲教程节切分写入 174 篇 `points/kp-*.md`（六节结构）；`kp-2-7` REST 试点手工正文保留。
- 索引：api-ref 条目 `status: 正式`；part1/2/3 原 `chapter` → `kind: legacy`、`status: 归档`。
- 站点：知识点类型筛增加「篇章（归档）」；知识地图注明 API 为主入口。

## 验收

- `/kb` →「考点（API）」任开一篇有要点正文；「篇章（归档）」仍可打开原章文件。
- `npm run sync:kb`、`npm run build`、`npm run check:release` 通过。

# 版本说明 v0.9.13

**日期**：2026-09-26

## 变更

- 考点参考（API 式）：自大纲对照表 bootstrap **174** 篇单页 `content/kb/points/kp-*.md`，索引分区 `api-ref`，每篇固定六节结构（见 `docs/kb-workshop/程序/知识点API参考模型.md`）。
- 脚本：`scripts/python/bootstrap_kb_api_points.py`（生成骨架、`--merge-index` 写入 kb-index）。
- 站点：知识点目录筛「考点（API）」、按篇 `group` 分组；阅读页显示大纲编号与 API 徽章；静态路由 `/kb/kp-*` 共 218 篇可搜。
- 试点正文：`kp-2-7`（Web 服务 / REST）已编制为正式。

## 验收

- `/kb` → 类型「考点（API）」→ 按章分组列表；打开 `kp-2-7` 六节完整。
- `npm run sync:kb`、`npm run build`、`npm run check:release` 通过。

## 后续

- 其余 173 篇仍为「待编制」骨架；可与旧篇章/速查并行，分批迁移正文至 `points/`。

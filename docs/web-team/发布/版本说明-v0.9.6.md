# 版本说明 v0.9.6

**日期**：2026-09-25  
**类型**：Patch（数据）

## 变更

- `ZT-2026上-案例05`（AI 智能体 Web 平台）：`domain` 需求 → **Web**，`chapter` 11 → **16**。
- 修复 `fix_case_domains.py` 仅在变更时写入 domain 的 bug（override 不生效）。

## 验收

- 案例页筛「需求」不含 2026 上试题五；筛「Web」可找到。

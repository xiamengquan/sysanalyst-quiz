# 版本说明 v0.9.21

**日期**：2026-09-26  
**知识点 API**：v1.2.6-api

## 变更要点

- 切分：`outlineRef` 仅在与教程章号一致时参与 ### 匹配，避免 15.1 误绑 4.1 维护占比。
- 锚点：删除错误的 `kp-9-4` 安全锚点；`kp-15-4` 更正为 `kp-13-4`。
- `supplement_raw` / 扩展 `FORCE_EXTRACT`：维护四类、MTBF、需求分析、问题分析等要点加厚。
- 门禁：`check-kb-points` 要求「要点」节 ≥80 字符（仍跳过手工 `kp-2-7`）。

## 验收

- [x] `python3 scripts/python/refine_kb_api_points.py`
- [x] `npm run sync:kb`
- [x] `npm run check:release`

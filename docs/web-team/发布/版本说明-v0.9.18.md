# 版本说明 v0.9.18

**日期**：2026-09-26

## 变更

- API 考点 v1.2.3：`finalize_definitions` / `dynamic_definition` 批量补全定义节；概述去重。
- 阅读体验：考点页内锚点目录、同 `group` 按大纲序 prev/next。
- `scripts/check-kb-points.mjs` 并入 `check:release`。

## 验收

- `npm run check:release`、`npm run build` 通过。

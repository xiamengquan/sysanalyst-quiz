---
description: 站点任何一点更新都要被记录并写入更新日志
alwaysApply: true
---

# 站点更新必记日志规范 (Changelog Always Recorded)

## 核心原则

**任何一点的更新都要被记录，写入更新日志。杜绝“静默更新”。**

无论是功能迭代、UI/样式微调、性能优化、代码重构、依赖变动、缺陷修复，还是知识点精炼、题库增补/校对，哪怕只有一两处改动，都必须在更新日志中留下明确、具体的文字记录，保证站点迭代历史透明、可溯。

## 记录与落地要求

1. **用户端更新日志（权威入口）**：
   - 必须在 `public/data/release-notes.json` 中，将本次更新内容写入对应版本（最新版本）的 `highlights` 数组中。
   - 语言简明扼要，直指改动点与用户收益（如：“重构：移除 shadcn 依赖，按 Cursor.com 极简现代风格全面重塑全站 UI”）。

2. **版本号与版本说明对齐**：
   - 若变动涉及功能、重构、样式体系或新增知识库维度，应适时推进 `package.json` 中的 `version`（如 `0.6.2` → `0.6.3`）。
   - 在 `docs/web-team/发布/版本说明-vX.Y.Z.md` 中同步归档详细的版本变更要点与验收记录。
   - 确保 `public/data/release-notes.json` 的 `latest` 与 `package.json` 的 `version` 严格一致。

3. **发版检查验证**：
   - 任何涉及更新日志的工作完成后，执行 `npm run check:release`，确保 release-notes、版本号与产物文件全部合规。

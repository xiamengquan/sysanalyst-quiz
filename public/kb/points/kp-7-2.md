# 数据安全与保密（加密、认证、KMI/PKI/SPK）

> **大纲**：9.2 · **教程**：第9章 · **分组**：科目1 · 7. 信息安全 · **状态**：工坊精修 v1.2.9 · 审计通过

## 概述

**数据安全与保密（加密、认证、KMI/PKI/SPK）**：数字签名（答卷·定义） 数字签名是基于非对称加密技术，用发送方私钥对消息摘要进行加密而形成的认证标识。 作用：用于验证发送方身份（鉴别）、保证消息未被篡改（完整性），以及防止发送方事后否认（不可否认）。

## 定义

- 加密算法（答卷·定义）：
- 数字签名（答卷·定义）
- **作用**：用于验证发送方身份（鉴别）、保证消息未被篡改（完整性），以及防止发送方事后否认（不可否认）。
- 数字证书（答卷·定义）
- **作用**：用于在 PKI 体系中绑定公钥与持有者身份，使通信各方能够验证公钥归属并建立可信通信。
- **数字签名**：数字签名是基于非对称加密技术，用发送方私钥对消息摘要进行加密而形成的认证标识。  
**作用**：用于验证发送方身份（鉴别）、保证消息未被篡改（完整性），以及防止发送方事后否认（不可否认）。
- **数字证书**：数字证书是由可信的证书颁发机构（CA）签发、符合 X.509 标准的电子凭证，内含用户身份信息、公钥、有效期及 CA 数字签名。  
**作用**：用于在 PKI 体系中绑定公钥与持有者身份，使通信各方能够验证公钥归属并建立可信通信。

## 要点

### 数据安全与保密

**加密 vs 认证**：

| 对比项 | 加密 | 认证 |
|--------|------|------|
| 目的 | 保密性 | 真实性、完整性 |
| 防攻击 | 被动(截获) | 主动(冒充/篡改/重放) |

| 类型 | 定义（答卷） | 作用（答卷） | 代表 |
|------|-------------|-------------|------|
| 对称(私钥) | 对称加密是加解密双方使用**同一密钥**的加密方式 | 用于大量数据的快速加密传输（如会话数据） | DES、IDEA、AES |
| 非对称(公钥) | 非对称加密是使用**公钥加密、私钥解密**（或反向签名）的加密方式 | 用于密钥交换、数字签名与身份认证等需区分公私钥的场景 | RSA(512+位) |
| 传统 | 传统加密是以代换、置换等手工规则变换明文的加密方式 | 用于理解加密基本原理（现代考试已不作为主流方案） | 简单，已淘汰 |
| Hash(摘要) | 摘要算法（Hash）是将任意长度输入映射为**固定长度、不可逆**摘要值的单向函数 | 用于完整性校验、口令存储与数字签名等（不能用于解密还原明文） | MD5、SHA |

**身份认证方式**：

| 方式 | 特点 |
|------|------|
| 口令 | 最常用，**极不安全** |
| 动态口令 | 一次一密，令牌 |
| 生物特征 | 指纹/虹膜/人脸 |

**密钥管理**：

| 方式 | 说明 |
|------|------|
| 点对点 | 对称/非对称 |
| KDC | 密钥分发中心 |
| PKI | 公钥基础设施，CA+证书 |

**数字信封**：对称密钥加密数据，公钥加密对称密钥。

## 易混辨析

（本节与相邻考点易混点见归档篇章「易混对比」；检索关键词：**数据安全与保密、KMI、PKI、SPK**。）

## 应试

**案例可能怎么问**：按场景选访问控制模型（DAC/MAC/RBAC/BLP/Biba）；防火墙类型与部署位置；给定 RPO/RTO 要求设计备份与容灾等级；N 版本/恢复块/防卫式程序容错对比。

**论文怎么用**：DevSecOps/安全运维类可写「左移测试+流水线门禁+密钥/权限最小化」；可靠性段落用 MTBF/可用性公式、冗余策略与 RPO/RTO 目标对齐业务连续性。

**常考结论**

- BLP：**上读下写**；Biba：**下读上写**（相反！）。
- 可用性 = **MTTF/MTBF × 100%**。
- 指数分布：**R(t)=e^(-λt)**，**MTTF=1/λ**。
- 串联：**R=∏Ri**；并联：**R=1-∏(1-Ri)**。
- 模冗余 **2n+1** 个模块，容忍 **n** 个故障。
- 等保 **5** 级：**自审标结验**。
- 口令认证**极不安全**；动态口令**一次一密**。
- N版本=**前向恢复/静态**；恢复块=**后向恢复/动态**。

- **本章练习热力**：教程第 9 章约 78 题（站点练习库，不含真题卷 ch=99）。
- **题库考点标签**：BLP模型、Biba模型、DAC、MAC、N版本程序设计。
- **练题入口**：[第 9 章练习（关键词 数据安全与保密）](/?bank=practice&chapter=9&path=all&q=%E6%95%B0%E6%8D%AE%E5%AE%89%E5%85%A8%E4%B8%8E%E4%BF%9D%E5%AF%86)

## 相关考点

- [信息系统安全体系](/kb/kp-7-1)
- [通信与网络安全（防火墙、VPN、SSL/HTTPS/IPSec、SSO/Kerberos）](/kb/kp-7-3)
- 归档通章：[`第09章-信息安全.md`](../第一篇-基础知识/第09章-信息安全.md)
